{
	"cmd": ["gcc", "-std=c99", "-O2", "-Wall", "-lm", "${file}", "-o", "${file_path}/a.out"],
	"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${file_path}",
	"selector": "source.c, source.c++",

	"variants":
	[
		{
			"name": "Run",
			"cmd": ["bash", "-c", "gcc -std=c99 -Wall -O2 -lm ${file} -o '${file_path}/a.out' && '${file_path}/a.out'"]
		}
	]
}
