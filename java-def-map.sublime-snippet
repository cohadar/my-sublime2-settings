<snippet>
    <content><![CDATA[
public static ${1:int}[] map(${3:String}[] S, Function<${3:String}, ${2:Integer}> f) {
	${1:int}[] A = new ${1:int}[S.length];
	for (int i = 0; i < A.length; i++) {
		A[i] = f.apply(S[i]);
	}
	return A;
}
]]></content>
    <tabTrigger>defArrayMap</tabTrigger>
    <scope>source.java</scope>
    <description>array transform</description>
</snippet>
