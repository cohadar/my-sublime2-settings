{
	"cmd": ["bash", "-c", "python '${file}' -s | python '${file}'"],
	"file_regex": "^[ ]*File \"(...*?)\", line ([0-9]*)",
	"selector": "source.python",

	"variants":
	[
		{
			"name": "Run",
			"cmd": ["bash", "-c", "python '${file}' -l | python '${file}'"]
		}
	]	
}
