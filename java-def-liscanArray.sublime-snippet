<snippet>
    <content><![CDATA[
static ${1:int}[] scanArray(Scanner scanner, int n) {
	String line = scanner.nextLine();
	Scanner arrayScanner = new Scanner(line);
	${1:int}[] A = new ${1:int}[n];
	for (int i = 0; i < n; i++) {
		A[i] = arrayScanner.next${1/.*/\u$0/}();
	}
	return A;
}
]]></content>
    <tabTrigger>defliscar</tabTrigger>
    <scope>source.java</scope>
    <description>line scan array</description>
</snippet>
